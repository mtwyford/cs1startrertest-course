title: CS1StartrerTest
description: this is a test.....
template:
  repo: cs1startrertest-template
  name: cs1startrertest
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
